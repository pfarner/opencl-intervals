Pull common Makefile contents into an imported file

Pull common *.cl functions (e.g. subdivision) into an imported file

Fix up dependencies, clean in Makefile

Why does degree get passed to Intervals when it's templated on that?
